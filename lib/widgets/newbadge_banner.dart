import 'package:flutter/material.dart';

class NewBadgeBanner extends StatelessWidget {
  const NewBadgeBanner({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 300,
      height: 100,
      color: Colors.white,
    );
  }
}